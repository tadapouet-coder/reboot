const workouts=['Haut du corps 20 min','Bas du corps 20 min','Cardio 25 min','Core 15 min','Mobilité 15 min'];function generateDay(){let s=+sleep.value,e=+energy.value,st=+stress.value,m=+motivation.value;if(s<=2||e<=2){today.innerHTML='<h3>Mode Survie</h3><p>10 min marche.</p>';return;}let score=Math.round(((s*30)+(e*30)+(m*20)+((6-st)*20))/5);let w=workouts[Math.floor(Math.random()*workouts.length)];today.innerHTML='<h3>Readiness '+score+'</h3><p>'+w+'</p>';let h=store.get('history');h.push(new Date().toLocaleDateString()+' '+w);store.set('history',h);renderHistory();}